query About($isVerified: Boolean!) {
  serverConfig {
    id
    about
    websiteURL
  }
  serverRules {
    ...Rule
  }
  serverRoles @include(if: $isVerified) {
    ...ServerRoleView
  }
  ratifiedProposalCount
  groupsCount
  voteCount
}
