query GroupSettingsByGroupId($groupId: Int!) {
  group(id: $groupId) {
    id
    settings {
      id
      adminModel
      decisionMakingModel
      ratificationThreshold
      reservationsLimit
      standAsidesLimit
      votingTimeLimit
      privacy
    }
  }
}
