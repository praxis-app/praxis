query GroupSettingsByGroupId($groupId: Int!) {
  group(id: $groupId) {
    id
    settings {
      id
      decisionMakingModel
      ratificationThreshold
      reservationsLimit
      standAsidesLimit
      votingTimeLimit
      privacy
    }
  }
}
