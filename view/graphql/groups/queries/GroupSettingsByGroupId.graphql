query GroupSettingsByGroupId($groupId: Int!) {
  group(id: $groupId) {
    id
    settings {
      id
      privacy
      ratificationThreshold
      reservationsLimit
      standAsidesLimit
    }
  }
}
