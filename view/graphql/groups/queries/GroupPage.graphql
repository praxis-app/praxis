query GroupPage($name: String!, $isLoggedIn: Boolean!) {
  group(name: $name) {
    ...GroupPageCard
    description
  }

  me @include(if: $isLoggedIn) {
    id
    serverPermissions {
      removeGroups
    }
    ...ToggleForms
  }
}
