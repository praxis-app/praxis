query Groups($offset: Int, $limit: Int, $isLoggedIn: Boolean = true) {
  groups(offset: $offset, limit: $limit) {
    ...GroupCard
  }
  groupsCount

  me {
    id
    serverPermissions {
      removeGroups
    }
  }
}
