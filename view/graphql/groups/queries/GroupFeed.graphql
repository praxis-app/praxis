query GroupFeed(
  $name: String!
  $offset: Int
  $limit: Int
  $isLoggedIn: Boolean!
) {
  group(name: $name) {
    id
    feed(offset: $offset, limit: $limit) {
      feedItems {
        ...FeedItem
      }
      totalCount
    }
  }
}
