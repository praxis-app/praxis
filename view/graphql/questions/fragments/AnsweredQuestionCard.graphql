fragment AnsweredQuestionCard on QuestionnaireTicketQuestion {
  id
  text
  priority
  likeCount
  commentCount
  isLikedByMe @include(if: $isLoggedIn)

  answer {
    id
    text
    user {
      id
      name
    }
  }
}
