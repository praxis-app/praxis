fragment MyAnsweredQuestionCard on Question {
  id
  text
  priority
  myAnswer {
    id
    text
    likeCount
    commentCount
    isLikedByMe @include(if: $isLoggedIn)
    user {
      id
      name
    }
  }
}
