fragment MyAnsweredQuestionCard on QuestionnaireTicketQuestion {
  id
  text
  priority
  answer {
    id
    text
    likeCount
    commentCount
    isLikedByMe @include(if: $isLoggedIn)
    user {
      id
      name
    }
  }
}
