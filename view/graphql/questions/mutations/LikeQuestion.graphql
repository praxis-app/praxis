mutation LikeQuestion($likeData: CreateLikeInput!, $isLoggedIn: Boolean!) {
  createLike(likeData: $likeData) {
    question {
      id
      likeCount
      isLikedByMe
    }
    like {
      id
      ...Like
    }
  }
}
