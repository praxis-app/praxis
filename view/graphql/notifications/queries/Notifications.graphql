query Notifications($offset: Int, $limit: Int) {
  notifications(offset: $offset, limit: $limit) {
    id
    actionType
    status
    createdAt

    otherUser {
      id
      name
    }
  }
  notificationsCount
}
