query Me {
  me {
    id
    ...UserAvatar

    serverPermissions {
      ...ServerPermissions
    }

    # TODO: Move this into another query - determine why this was added
    joinedGroups {
      id
      name
    }
  }
}
