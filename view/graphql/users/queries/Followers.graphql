query Followers($name: String!, $offset: Int, $limit: Int) {
  user(name: $name) {
    id
    followerCount
    followers(offset: $offset, limit: $limit) {
      nodes {
        ...Follow
      }
      totalCount
    }
  }
  me {
    id
  }
}
