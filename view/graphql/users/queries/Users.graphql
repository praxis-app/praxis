query Users($offset: Int, $limit: Int) {
  users(offset: $offset, limit: $limit) {
    nodes {
      ...UserEntry
    }
    totalCount
  }
  me {
    id
    serverPermissions {
      removeMembers
    }
  }
}
