query HomeFeed($offset: Int, $limit: Int, $isLoggedIn: Boolean = true) {
  me {
    id
    homeFeed(offset: $offset, limit: $limit) {
      feedItems {
        ...FeedItem
      }
      totalCount
    }
  }
}
