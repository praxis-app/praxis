query Following($name: String!, $offset: Int, $limit: Int) {
  user(name: $name) {
    id
    followingCount
    following(offset: $offset, limit: $limit) {
      nodes {
        ...Follow
      }
      totalCount
    }
  }
  me {
    id
  }
}
