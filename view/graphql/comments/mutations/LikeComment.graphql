mutation LikeComment($likeData: CreateLikeInput!) {
  createLike(likeData: $likeData) {
    like {
      id

      # TODO: Determine why cache is not updating for the below
      # comment {
      #   id
      #   likeCount
      #   isLikedByMe @include(if: $isLoggedIn)
      # }
    }
  }
}
