fragment ProposalAction on ProposalAction {
  id
  actionType
  groupDescription
  groupName

  groupConfig {
    id
    privacy
  }

  event {
    ...ProposalActionEvent
  }

  role {
    ...ProposalActionRole
  }

  groupCoverPhoto {
    ...AttachedImage
  }
}
