export namespace Votes {
  export enum VotingTypes {
    Consensus = "consensus",
    XToPass = "x-to-pass",
    Majority = "majority",
  }

  export enum FlipStates {
    Up = "up",
    Down = "down",
  }
}
